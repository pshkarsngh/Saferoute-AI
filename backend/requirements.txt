# SafeRoute AI — backend runtime dependencies (Phase 1)
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1
sqlalchemy==2.0.36
httpx==0.28.1
psycopg[binary]==3.2.3          # PostgreSQL driver (15_DEPLOYMENT.md / docker-compose)

# Optional heavy stacks (lazily imported; gracefully unavailable when missing):
# opencv-python-headless==4.10.0.84   # OpenCV preprocessing (09_ML_SPEC.md)
# ultralytics==8.3.45                 # YOLO11 inference (09_ML_SPEC.md)

# Dev/test
pytest==8.3.4
ruff==0.8.6
mypy==1.14.1